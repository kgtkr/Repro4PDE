package net.kgtkr.seekprog;

import processing.app.tools.Tool
import processing.app.Base

class ArtStation() extends Tool {

  def getMenuTitle() = {
    "##tool.name##"
  }

  def init(base: Base) = {}

  def run() = {}
}
